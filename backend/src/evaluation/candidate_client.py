"""
Returns an answer generated by our model for a given question
"""
from typing import Dict

# Mocked model outputs - replaced once model can be properly queried
# Are keyed using the question
_MOCK_CANDIDATES: Dict[str, str] = {
    "How do I reset my account password?": (
        "Use the 'Forgot password' link on the sign-in page. "
        "You'll receive an email with a reset link."
    ),
    "How long do refunds take?": (
        "Once approved, refunds typically show up in about a week (around 5 to 7 business days)."
    ),
    "Can I change my subscription plan?": (
        "Yes—open your account settings, go to subscription, and select a different plan. "
        "It takes effect right away."
    ),
}


def get_candidate_answer(base_url: str, question: str, document_id: int) -> str:
    """
    Gets an answer (candidate) from our own model for a given question

    :param base_url: The API that is used to prompt our model
    :type base_url: str
    :param question: The question that the model will be asked
    :type question: str
    :param document_id: The ID of the document that the question is from
    :type document_id: str
    :return: The answer outputted by our model
    :rtype: str
    """
    return _MOCK_CANDIDATES.get(
        question,
        "I'm not sure. Please contact support for more information."
    )
